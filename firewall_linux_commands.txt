
# Enable UFW
sudo ufw enable

# Check status
sudo ufw status verbose

# Block Telnet (port 23)
sudo ufw deny 23

# Test connection
telnet localhost 23

# Allow SSH
sudo ufw allow 22

# Remove Telnet rule
sudo ufw delete deny 23
